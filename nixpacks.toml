[phases.setup]
nixPkgs = ["nodejs_20", "python3"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = [
  "mkdir -p /app/data /app/logs",
  "node scripts/setup.js"
]

[start]
cmd = "node src/index.js"

[variables]
NODE_ENV = "production"
